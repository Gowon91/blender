"""
Autogenerated Blender Add-on entry module.

This Jinja2 template renders the top-level Python module for the add-on,
providing bl_info and registration hooks. Variables like ADDON_NAME and
others are replaced by the generator at build time.
"""

bl_info = {
    "name": "{{ADDON_NAME}}",
    "author": "Generated",
    "version": (0, 1, 0),
    "blender": (3, 5, 0),
    "location": "3D Viewport > Sidebar > IDS",
    "description": "Populate IFC Psets/Properties from IDS rules (c-1/c-2)",
    "category": "Import-Export",
}

import bpy
from . import ui as _ui
from . import ops as _ops
from . import assign as _assign

classes = (
    _ui.IDS_PT_rules,
    _ops.IDS_OT_refresh_cache,
    _ops.IDS_OT_rule_search,
    _ops.IDS_OT_rule_add_pset,
    _ops.IDS_OT_rule_execute,
    _assign.IDS_OT_assign_open,
    _assign.IDS_OT_assign_set,
    _assign.IDS_OT_assign_clear,
    _assign.IDS_OT_assign_save,
)

def register() -> None:
    """Register all classes defined in this add-on."""
    for cls in classes:
        bpy.utils.register_class(cls)

def unregister() -> None:
    """Unregister all classes defined in this add-on."""
    for cls in reversed(classes):
        bpy.utils.unregister_class(cls)